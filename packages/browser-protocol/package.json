{
  "name": "@chijie/browser-protocol",
  "version": "0.1.13",
  "description": "Model-agnostic browser observation/action/receipt protocol: types, zod schemas, serialization, versioning, pure functions",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "types": "index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "pnpm lint --fix",
    "prettier": "prettier . --write --ignore-path ../../.prettierignore",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@extension/tsconfig": "workspace:*",
    "typescript": "5.5.4",
    "vitest": "2.1.9"
  }
}
