---
title: "长程任务 Agent 北极星"
description: "持节的核心竞争力从浏览器操作演示转向长程复杂任务：一次委托、计划、长上下文、自主执行、可验证交付。"
category: "product"
number: "021"
status: current
services: ["docs", "projects/chijie-browser"]
related:
  - "product/003"
  - "product/019"
  - "product/020"
  - "decisions/004"
last_modified: "2026-08-11"
---

# 021 — 长程任务 Agent 北极星

## 状态

**current，Owner 已确认。** 本文件取代旧的“浏览器操作演示 + 强制审批”叙事。旧方向保留为历史文档，不再作为当前北极星。

## 一句话

持节是一个运行在用户 Chrome 里的**长程任务 Agent**：用户交出一个自然语言目标，Agent 自己拆计划、跨页面/站点执行、记住上下文、完成可验证交付。

## 用户要看到的效果

1. 用户只给一次任务，例如“调研 10 家竞品，输出对比表和一份结论”。
2. Agent 先给出简短计划，然后开始自主执行。
3. 执行过程中展示当前阶段、已找到的关键结果、下一步，不要求用户逐动作确认。
4. 任务完成后给出可检查的成果：表格、报告、文件、页面状态或回执。
5. 用户随时可以停止、追问、修正；停止不代表每一步都要批准。

## 能力定义

### 长程

- 任务可能跨多个页面、多个站点、多轮观察，长度以“目标完成”为准，不以单页面操作为边界。
- 必须有持久化计划：阶段、子目标、检查点、验收条件。
- 中断后应能从计划中恢复，而不是从零开始。

### 复杂

- 需要多步推理、信息收集、结果整理和交付物生成。
- 不是“打开某个页面”或“暂停视频”这类单动作捷径。
- 单动作捷径可以作为内部工具，但不是产品核心。

### 自主

- 任务级授权：用户提出目标即授权目标内的行动。
- 不弹强制批准，不因“外部提交”打断。
- 超范围高风险动作才说明；敏感输入仍直接拒绝自动处理。

### 可验证

- 中间检查点和最终结果都必须有可观察证据。
- 模型说“完成”不算完成；证据不足时诚实失败。

## 与旧方向的关系

| 旧方向 | 新方向 |
|---|---|
| 浏览器行动 Agent，单轮任务环 | 长程任务 Agent，跨阶段执行 |
| Tabbit 91.8% 为首要对齐目标 | 对齐目标退化为质量参考，不以浏览器操作演示为北极星 |
| 外部提交必须批准 | 任务内自主执行，超范围才说明 |
| 计划/记忆是后续可选 | 持久计划与上下文记忆是核心部件 |
| Claw 30 为强制产品对标 | Claw 30 降为历史评测资产 / 参考集 |

## 已落地（2026-08-06 证据）

1. 默认审批阻塞已取消；外部提交直接执行（decision 004）。
2. Mission/Plan：阶段骨架、持久化、侧栏阶段展示。
3. 长程上下文：轨迹窗口 + plan memory（确定性 archive；非 LLM 语义压缩）。
4. 全量 ACI 与 prompt 版本 `chijie-control-v0.3.0`。
5. 长程迷你集 `021-LH-01..03` 注册于 `product/020`；正式批 MiniMax-M3 **10/10 verified_pass**（含 LH-01..03）。
6. Outer-loop 已生成 Skill 候选（`reports/nanobrowser/outer-rl/skills/candidates/`）。

证据：`reports/nanobrowser/agent-book-iteration/2026-08-06-iteration-report.md`。

## 当前缺口

1. 上下文压缩仍是确定性 archive，不是 LLM 语义摘要。
2. 跨中断的完整任务恢复协议仍偏薄（plan 持久化有了，恢复体验未封顶）。
3. 真站长尾（飞书/B 站等）仍需 Owner 登录态；与旧 M3 黄金旅程相关，**不是**当前北极星本身。
4. 外环 Skill 候选需继续质量筛选与扩跑，不是“方案未动”。

## 实施顺序

1. **已完成：** 取消默认审批阻塞，外部提交直接执行。
2. **已完成：** Mission/Plan 数据模型、持久化与侧栏阶段展示。
3. **已完成（v1）：** 长程上下文压缩与计划记忆（确定性）。
4. **已完成（迷你集）：** 长程评估 `021-LH-*` 与正式批证据。
5. **下一项：** 长程 eval harden + 外环候选质量；语义压缩与恢复协议继续加深。

## 非目标

- 不新造浏览器。
- 不做云端 Agent 平台。
- 不做桌面宠物、妙招广场等外围功能。
- 不做没有证据的长任务“看起来完成”。
- 不把 Claw 30 / 旧 parity 重新升为产品北极星。

## 参考

- `decisions/004`：任务内自主执行
- `product/019`：Agent 建设规划中的 Harness 方法
- `product/020`：评估主表
