HANDOFF|task=g3-011|status=delivered|files=projects/chijie-browser/chrome-extension/src/background/agent/backends/control-llm.ts,projects/chijie-browser/chrome-extension/src/background/task/__tests__/manager.test.ts,projects/chijie-browser/pages/side-panel/src/presentation/__tests__/failure-taxonomy.test.ts|tests=pnpm -F @extension/sidepanel test -- src/presentation/__tests__/failure-taxonomy.test.ts:0; pnpm -F chrome-extension test -- src/background/task/__tests__/manager.test.ts:0|unverified=no browser side-panel screenshot; no Feishu 06; G4 independent reverify pending

# g3-011 delivery notes

## Done
- control-llm `toExecutorOutcome`: failed categories including `no_progress` / `max_steps` pass through as `{kind:failed, category}` (not swallowed).
- control-llm already passes explicit `maxNoProgress=3` into `runObserveActLoop` (010).
- TaskManager already maps failed → status failed + `round.failureCategory` (unchanged; covered by tests).
- failure-taxonomy already maps `no_progress`/`max_steps` → product `model_loop` (E1 strengthened + chrome.i18n stub for unit env).
- E2: manager tests for `no_progress`, `max_steps`, and `waiting_user` stop surfaces.

## Commands (cwd: projects/chijie-browser)
```
pnpm -F @extension/sidepanel test -- src/presentation/__tests__/failure-taxonomy.test.ts   # exit 0 (5 tests)
pnpm -F chrome-extension test -- src/background/task/__tests__/manager.test.ts               # exit 0 (35 tests)
```

## Not done
- Real side-panel click/screenshot (out of contract gate; G4/L3)
- Contract body / LIVE board edits (G1/G2 only)
