# Plan

1. Add public-seam regression coverage for `getCurrentPage()` and `getTabInfos()`.
2. Reuse `isUrlAllowed()` inside `BrowserContext` for selection and state filtering.
3. Verify, sync the two changed source/test files, update the dated report, commit, review, and run main-Chrome E2E.
