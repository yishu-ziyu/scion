# 持节修复暂停交接

**暂停时间：** 2026-08-13

**暂停原因：** Owner 要求先停下，完整记录已做工作与后续计划。

**当前判断：** A/B/C 已完成。D 短测在 `ff3b077` 上 **5/6 诚实通过**，不是发布通过。021-LH-04 仍诚实失败。主 Chrome 原生侧栏未跑。E/F 未开始。不得把 DF-P0 标成 DONE。

## 1. 当前精确停点

- 仓库：`/Users/mahaoxuan/Desktop/AI产品经理/自研产品/scion`
- 分支：`main`，**领先 origin 5 个提交，未推送**
- 当前 HEAD：`ff3b07759d164081d2347413e99e15ef4be13f33`
- 本轮 D 迭代新提交（均未推送）：
  - `5aa9033` 完成必须有页面证据
  - `b4033ea` 页面正文大小写
  - `5a7d569` 长程提取与 IANA 途经跳转
  - `57a3397` 商品 CSV 换行 + 空 oracle 行
  - `ff3b077` 双来源途经跳转不再锁 IANA 完成准则
- 不要 `git add -A`。`.omo/`、`clicky/`、旧评测产物保持未跟踪。
- `current_milestone`：`long_horizon_v1`（021）。

## 2. 本轮已经做了什么

### 2.1 完成真实性与长任务

1. 完成不再只相信模型说“完成”，必须有当前任务、当前轮次、当前页面的可检查证据。
2. 页面正文回答、日期、价格、作者、表格等内容必须能回到实际页面证据；只有网址或标题不能冒充正文答案。
3. 长任务计划在验收时收敛为“可证明阶段 + 必要时的最终输出阶段”，不再让无证据的叙事步骤永久卡住，也不放宽最终收据。
4. 执行器每一步重新读取当前计划，避免计划已前进但执行仍停留在旧阶段。
5. 多网址任务保留完整出现顺序，包括 `A → B → A`；最后网址作为当前页面要求，前面的网址由真实访问记录证明。
6. 多来源回答必须分别包含各来源的实质内容、正确顺序与当前页面证据；重复来源、逆序、只有标题/网址、伪造引文均拒绝。

### 2.2 商品表格与最高价任务

1. 修复“列表页”被误识别成表格字段的问题；显式 `name,price,rating` 才作为字段要求。
2. 表格证据按整行绑定，不再允许把 A 商品的名称和 B 商品的价格拼成一行。
3. 要求全部商品时，交付行必须唯一且覆盖页面实际提取集合；重复一行并漏掉另一行会失败。
4. 最高价从已经整行核实的数据确定性计算，并保留页面原价格文本；不能由答案自己证明自己。
5. 评测区分 R1 表格提取与 LH-03：R1 接受真实 CSV/Markdown 表格，LH-03 还必须给出正确最高价。

### 2.3 会话、侧栏与控制

1. 修复新对话、技能启动、暂停/继续、停止、历史会话切换期间的多处串话和永久锁住问题。
2. 新任务只绑定当前可用标签页；普通后续回复沿用既有任务绑定，不会借用后台错误标签页。
3. 任务卡、轮次与完成收据带明确归属；旧任务卡、旧收据或另一个轮次不能被当前任务误认。
4. 历史会话保持只读，迟到事件不能写进当前会话。
5. 等待、停滞、恢复、失败与完成状态的展示和控制已补充回归覆盖。

### 2.4 评测可信度

1. 评测记录绑定任务、轮次、标签页、运行状态、侧栏状态与完成收据；页面成功不能替侧栏伪造结果。
2. 失败任务可以诚实计入结果，但失败时不得暗藏完成收据，也不得把旧任务身份写进当前失败记录。
3. 快速从“未开始”直接到“完成”的任务可以被识别，不再强制必须采样到中间状态。
4. 初次打开网页只对一次明确的临时断线做同网址单次重试；不会换网站、吞错误或把失败算成功。
5. 访问记录保留“离开后回到同一网址”的新事件，连续停留在同一页的重复观察才合并。
6. 评测的商品、多来源和动作任务分别使用各自合同；自我否定、臆测、状态不一致和收据归属错误会失败。
7. 既有评测可信链已加入本机签名、候选版本、运行身份与证据闭包检查；正式浏览器批次仍待新提交后执行。

### 2.5 最后一处测试不稳定

`product-table-journey.test.ts` 原来用约一秒轮询等待完成，完整测试较忙时会看到任务仍在运行并偶发失败。现改为：

- 在启动任务前订阅真实的“已核实完成”事件；
- 同时核对任务、轮次与收据；
- 4 秒只用于发现死锁，不作为成功判定窗口；
- 未改产品代码。

该路径定向连续运行 20 次均通过，随后也进入完整扩展测试并通过。

## 3. 暂停前已有证据

| 检查 | 暂停时结果 | 备注 |
| --- | --- | --- |
| 商品完整旅程重复运行 | 20/20 PASS | 等真实完成事件，不靠固定等待 |
| 扩展完整测试 | 65 文件 / 807 项 PASS | 暂停时候选 |
| 评测规则测试 | 99/99 PASS | 暂停时候选 |
| 侧栏完整测试 | 17 文件 / 252 项 PASS | 停止时自然完成，退出码 0 |
| `git diff --check` | PASS | 无行尾/补丁格式错误 |
| 最终类型、规范、格式、构建八门 | **当时未完成** | 见下方 A/B 恢复结果 |
| 最终真实性两轮反攻 | **当时已中止** | 见下方 A/B 恢复结果 |
| 最终范围/简洁性审查 | **当时已中止** | 见下方 A/B 恢复结果 |
| 主 Chrome 原生侧栏复测 | **未执行** | 自动测试不能替代；属步骤 D |
| 新正式浏览器多轮评测 | **未执行** | 旧失败批次不能包装成新通过；属步骤 E |

因此 Todo 中所有 `VERIFYING` 条目保持原状态，没有任何条目被提前改成 `DONE`。

## 3.1 恢复 A/B 结果（2026-08-13）

**A 文件核对：** 分支 `main`，HEAD `88eb23126dde22cf4ef8925b178510f790ac8a96`，未暂存。暂停指纹 `f77f76…` 与五个新文件 SHA 全部匹配。无第三方继续改代码的迹象。`.omo/`、`clicky/`、旧评测产物未被碰触。

**B 反攻（真实问题 vs 误报）：**

| 判定 | 位置 | 说明 |
| --- | --- | --- |
| 真实缺陷（已修） | `product-table-journey.test.ts` 原写法 | `unsubscribe` / `TaskEvent` 未收窄，`tsc` 失败。只改测试类型。 |
| 真实缺陷（已修） | `instruction-language.ts` 导航「返回」 | 原先只排除「返回上一页」；`返回首页/列表/商品页` 会误开交付门。已加宽排除，并补回归。`返回页面标题` 仍是交付。 |
| 真实缺陷（已修） | `eval-public-task.mjs` / `eval-frontier-task.mjs` catch | `wrong_tab: wrongTab ?? 0` 在无法观测 tab 时撒谎。已改为 `?? ''`，与 action runner 一致；harness 源码锁禁止 `?? 0`。不产生 `verified_pass`，但 provenance 不再默认 0。 |
| 残余，不阻断 C，且不宣称关闭 | `manager.test.ts` DF-P0-01 正例 | 正例把「概括+引用」收成同一句引语，弱于 dogfood 验收的两段交付。标题/域名假完成已被拒。条目保持 VERIFYING。 |
| 残余，不阻断 C，且不宣称关闭 | `deriveInstructionUrlPlan` 只冻末 URL | 有序多源时当前页准则是最后网址，前面的由访问记录证明。与 DF-P0-03「每阶段独立 Gate」仍有差距；交付合同拦住末 URL 假完成。条目保持 VERIFYING。 |
| 误报 | `eval-action.test.mjs` 对 runner 源码做正则 | 与 redteam/harness 行为测试互补。现已另锁 public/frontier 不得 `wrong_tab ?? 0`。 |
| 误报 | `productTableCompletionPlan` 写死 CSV | 仅测试辅助。 |
| 残余风险，不阻断 C | `manager.ts` Artificial intelligence freeze | 评测过拟合；额外条件偏严。 |

`instruction-language.ts`：子句切分 + 否定奇偶 + 施事 + 目标 + 网址顺序。导航「返回首页/列表/商品页」不再当作交付；「返回页面标题」仍是交付。

**B 检查数字（含反攻后两处最小修复）：**

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 商品完整旅程 ×20 | 连续 20 次 journey 文件 | **20/20 PASS**（每次 3 tests；类型修复后） |
| 扩展完整测试 | `pnpm -F chrome-extension test` | **65 文件 / 815 项 PASS**（新增 8 条导航回归） |
| 评测规则测试 | `node --test scripts/__tests__/eval-*.test.mjs` | **99/99 PASS** |
| 侧栏完整测试 | `pnpm -F @extension/sidepanel test` | **17 文件 / 252 项 PASS** |
| 类型检查 | extension + side-panel `tsc --noEmit` | PASS |
| prettier --check 变更文件 | PASS |
| 构建 | extension + side-panel vite build | PASS；`dist/` 不得提交 |

**步骤 C 放行：GO（提交候选，不关闭 dogfood）。** 独立审查若把目标理解成「宣称 DF-P0 已关」则为 NO-GO；本交接从未把 VERIFYING 改成 DONE。精确暂存，禁止 `git add -A`。

步骤 D–F 仍按第 5 节第三步之后的原文执行（主 Chrome 短测、正式多轮、按证据关闭）。

## 4. 明确未完成与剩余风险

1. 当前改动较大且多人曾同时修改任务管理与评测文件；必须做一次冻结版本上的独立范围审查。
2. `instruction-language.ts` 虽替代了多处散落判断，但 400 行仍需确认主要是结构解析，而不是句式补丁堆积。
3. 自动测试全绿不能证明主 Chrome 里的简单、中等、长任务真的成功，也不能证明暂停/继续/停止和新对话的可见体验正确。
4. 当前未跟踪的旧评测 CSV、`artifacts/`、`build-attestations/` 是过程产物。不得混入候选提交，也不得把旧失败批次改名成新通过。
5. `.omo/` 与 `clicky/` 明确不在本任务范围；`clicky/` 内还有自己的工作区与本地秘密。两者都不得暂存或修改。
6. 公开 Git 历史中的提交 `954e9e6` 仍含已删除的私有 Notion 链接。当前文件树已无该泄漏，但是否改写公开历史必须由 Owner 明确决定；在此之前 DF-P0-10 继续 `BLOCKED`。
7. `eval-new` 宿主在当前环境没有提供所需入口，因此没有执行，也不能用普通浏览器或自造脚本冒充真人模拟结论。

## 5. 恢复后的固定执行顺序

### 第一步：确认没有漂移

1. 进入同一仓库，读取 021、Todo 与本交接。
2. 检查分支、提交、`git status`、代码差异指纹和五个新文件 SHA。
3. 确认 `.omo/`、`clicky/` 与旧评测产物仍未被碰触。
4. 若代码指纹已变化，之前的最终验收全部失效，重新冻结新候选。

### 第二步：同一候选完成三路只读放行

1. **L3 / Sol — 真实性反攻：** 两轮固定反例与合法等价；可构造假完成必须为 0，真实完成误拒必须为 0。
2. **L3 / Sol — 范围与简洁性：** 审查全部差异，重点判断 400 行语言模块、任务管理、评测证据与侧栏身份是否都对应本轮真实问题；有无关或重复实现就先删。
3. **L1 / Terra — 机械检查：** 只读执行评测测试、扩展测试、侧栏测试、类型检查、规范检查、变更文件格式检查、`git diff --check` 与构建。
4. **L3 / Sol — 监督：** 核对三路确实使用同一指纹；只有三路均通过才放行下一步。

每次派 Agent 都必须写清：等级、理由、文件所有权、停止条件和验证方式。常规边界明确的修复才交给 **L2 / Luna**；机械工作不用高价模型，高风险真假判断不用低档模型。

### 第三步：形成可构建的候选提交

1. 精确检查差异和暂存清单，禁止 `git add -A`。
2. 只暂存本轮源码、测试及明确的交接/验收文档；排除 `.omo/`、`clicky/`、旧评测 CSV、`artifacts/`、`build-attestations/` 和本地秘密。
3. 提交并推送一个候选版本。正式评测需要可重建的干净提交，不能以脏工作区作为正式基准。

### 第四步：新构建的真实短测

1. 执行 `pnpm eval:build`，确认打包内容与候选提交一致。
2. 新建独立 campaign，先各跑 1 次：`013-A01`、`013-B07`、`021-LH-01`、`021-LH-02`、`021-LH-03`、`021-LH-04`。
3. 要求每个任务真实成功，且 `false_complete=0`、`wrong_tab=0`、会话污染为 0；不能只看总门通过。
4. 任一失败立即回到“定位根因 → 最小修复 → 小检查 → 完整检查 → 新 campaign”，不得覆盖旧证据。

建议短测命令骨架（恢复时按 020 再核对变量）：

```bash
cd projects/chijie-browser
MATRIX_STAMP=<new-campaign> \
TASKS=013-A01,013-B07,021-LH-01,021-LH-02,021-LH-03,021-LH-04 \
RUNS=1 \
MINIMAX_MODEL=MiniMax-M3 \
POLICY_TAG=verified-long-horizon-v1 \
pnpm eval:matrix
```

### 第五步：主 Chrome 原生侧栏验收

使用主 Chrome 和实际安装的扩展，逐项保存可观察证据：

1. 简单读取：不能仅凭标题/网址假完成，必须返回页面事实。
2. 中等导航：从示例页到 IANA，成功导航不能被错误判失败。
3. 长任务：IANA → Wikipedia，两来源、顺序、引文和最终交付都正确。
4. 商品任务：完整表格与最高价均来自真实页面行。
5. 新对话：旧任务、旧消息、旧状态不得污染。
6. 暂停 → 继续、等待用户、被打断、停止：按钮有效，状态和结果一致。
7. 430px、320px、200%、键盘与 VoiceOver：无阻断缺陷。

### 第六步：正式多轮与关闭 Todo

1. 主 campaign 最少 6 个注册任务 × 3 次，共 18 行，并逐任务报告结果和失败原因。
2. 按 020 与 DF-P0-07 再执行独立 arm/campaign 的 3 次对照；不得把两个 arm 混成一个 campaign。
3. 保存新摘要、过程记录、失败原因与主 Chrome 证据；旧失败批次保留原样。
4. 只有满足每个条目的原路径、自动回归、可见证据和精确提交后，才把 `VERIFYING` 改为 `DONE`。
5. DF-P0-10 必须等待 Owner 对公开历史的明确决定，不得擅自改写或强推。
6. 完成文档与证据的最后提交、推送，并观察远端检查结果。

## 6. 下次开始时的一句话

> D 未绿。HEAD `ff3b077`（5 个提交未推送）。六任务短测 5/6：LH-04 诚实失败。不要宣称 DF-P0 已关。E 未开始。主 Chrome CDP `health: BAD`，不要 `chrome-cdp repair` 除非 Owner 允许退出 Chrome。

## 7. D 恢复结果（2026-08-13）

**官方模型：** MiniMax-M3。**attach：** Chrome for Testing。**policy：** `verified-long-horizon-v1`。

最新可引用 campaign（自我校验通过，6/6 行，`false_complete=0`，`wrong_tab=0`）：

- stamp：`2026-08-13-ff3b077-d-smoke`
- git：`ff3b07759d164081d2347413e99e15ef4be13f33`
- CSV：`reports/nanobrowser/eval/2026-08-13-ff3b077-d-smoke-eval-matrix.csv`
- 摘要：`reports/nanobrowser/eval/2026-08-13-ff3b077-d-smoke-eval-summary.md`
- campaign：`reports/nanobrowser/eval/2026-08-13-ff3b077-d-smoke-eval-campaign.json`
- 产物：`reports/nanobrowser/eval/artifacts/2026-08-13-ff3b077-d-smoke/`

| task_id | attempt | outcome | 说明 |
| --- | --- | --- | --- |
| 013-A01 | 1 | verified_pass | Wikipedia 标题/域名，有页面证据 |
| 013-B07 | 1 | verified_pass | 到达 IANA Example Domains |
| 021-LH-01 | 1 | verified_pass | `wiki/Artificial_intelligence` |
| 021-LH-02 | 1 | verified_pass | Wikipedia Web browser 正文 |
| 021-LH-03 | 1 | verified_pass | 6 行 CSV + 最贵 Beta Mechanical Keyboard $89.00；交付保留换行 |
| 021-LH-04 | 1 | fail `agent_failed` | 技能已 example→IANA→Wikipedia 且停在 Wikipedia；模型未写出观察一/观察二。诚实失败，无完成收据 |

更早 D 批次保留原样，不得改名成通过：`5aa9033-d-smoke`、`b4033ea-d-smoke`、`5a7d569-d-smoke`（LH-03 曾 `false_complete=1`，评测误杀）、`57a3397-d-smoke`（LH-03 已绿，LH-04 仍失败）。

**D 迭代修了什么：**

1. 侧栏交付不再把 CSV 换行压成空格；评测 oracle 忽略空的 `product-list` 包装行。LH-03 从假完成变成真实通过。
2. 双来源「More information」途经跳转不再把 IANA URL 冻成完成准则；并按指令中的 Wikipedia 条目继续走。LH-04 不再 IANA↔Wikipedia 来回，但仍未交付。

**回归（`ff3b077`）：** journey 20/20；extension 820；eval 99；side-panel 253；tsc/prettier 绿。

**未做 / 阻断：**

1. D 未诚实全绿 → **不得进入 E**（正式 3 轮）。
2. 主 Chrome 原生侧栏：`chrome-cdp status` → `listen: yes`，`targets: 0`，`DevToolsActivePort: missing`，`health: BAD`。PID **55713** 占用 `127.0.0.1:9222`。`ensure` 无法接管；`repair` 会退出主 Chrome，需 Owner 允许。
3. LH-04 剩余缺口是模型在两站都已访问后仍不写「观察一/观察二」。再做一个评测五段格式技能会变成评测过拟合；按连续失败规则停止对该缺口的产品补丁。
4. F：全部 dogfood 仍为 VERIFYING。DF-P0-10 仍等 Owner 决定是否改写公开历史。

**Owner 可解锁：**

1. 允许 `chrome-cdp repair`（会退出主 Chrome）或自行腾出 9222，才能跑原生侧栏第五步。
2. 决定 LH-04：接受 MiniMax-M3 在该合同上的诚实失败，或另开产品方向（不是把评测五段格式写进技能）。
3. DF-P0-10 公开历史是否重写。
