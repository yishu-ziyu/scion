{
  "fixture": "multi-source",
  "entry": "index.html",
  "seedParam": "seed",
  "defaultSeed": "42",
  "oracle": {
    "global": "window.__CHIJIE_FIXTURE_STATE__",
    "fields": ["fixture", "seed", "openedSource", "openedPosition", "openedItemId", "sources", "itemsPerSource"]
  },
  "reset": "window.__CHIJIE_FIXTURE_RESET__()",
  "notes": "Two result sources with per-source seeded scores. Position identity (source, position) is the oracle; text is intentionally non-unique across seeds. Supports 'open the Nth result of source X' resolution tests."
}
